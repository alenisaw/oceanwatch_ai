# Supabase

Supabase is optional for the MVP.

Use it for:

```text
incident history
stored JSON reports
review workflow
shared team dashboard state
```

Apply migrations manually until deployment is stable.
